---
interface Props {
  current?: string;
}

import Logo from "./Logo.astro";
import Nav from "./Nav.astro";

const { current = "" } = Astro.props;
---

<header class="flex mx-auto my-0 w-full max-w-screen-xl">
  <Logo />
  <Nav current={current} />
</header>
